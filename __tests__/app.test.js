//Oh brother.  I just don't know... and I'm running out of time....

const rawRequest = require('../app.js');

describe('parseRequest', () => {
  it('parses a raw request for method', () => {
    const parsedRequest = parseRequest(rawRequest);
  });
});
